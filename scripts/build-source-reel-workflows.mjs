import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';

const root = 'C:/dev/n8n-youtube-shorts-automation';
const template = JSON.parse(fs.readFileSync(path.join(root, 'workflows/n8n_하루건강약사_수동실행.json'), 'utf8'));
const longevityTemplate = JSON.parse(fs.readFileSync(path.join(root, 'workflows/n8n_geongangjangsubigyeol_manual.json'), 'utf8'));
const keep = [
  'Use Live Image?','KIE Create Image Task','Normalize Image Task','Wait Image 30s','KIE Get Image Task','Parse Image Result','Image Ready?',
  'Wait Image Retry 30s','Prepare Image Retry Poll','KIE Get Image Task Retry','Parse Image Result Final','Image Task Retryable?',
  'Wait Image Task Retry 30s','Prepare Image Task Retry','Mock Image Result','Use Live BGM?','KIE Create BGM Task','Normalize BGM Task',
  'Wait BGM 30s','KIE Get BGM Task','Parse BGM Result','BGM Ready?','Wait BGM Retry 90s','KIE Get BGM Task Retry','Parse BGM Result Final',
  'Mock BGM Result','Use Live Render?','Prepare Local FFmpeg Render','Local FFmpeg Render','Parse Local Render Result','Read Rendered MP4',
  'Attach Downloaded MP4','Allow YouTube Upload?','YouTube Upload Public','Normalize YouTube Upload','Skip YouTube Upload','Mock Render Result',
  'Post Top-Level Comment','Attach Comment Result',
];

function loadCode(channelId, topicDir, displayName) {
  return `const fs=require('fs'); const path=require('path');
const incoming=$input.first()?.json||{}; const topicDir=${JSON.stringify(topicDir)}; const channelId=${JSON.stringify(channelId)};
const bundleRoot='C:/dev/n8n-youtube-shorts-automation/data/source-reel-bundles';
const staleClaimMs=12*60*60*1000;
if(fs.existsSync(bundleRoot)){for(const entry of fs.readdirSync(bundleRoot,{withFileTypes:true})){if(!entry.isDirectory())continue; const bundle=path.join(bundleRoot,entry.name); const mf=path.join(bundle,'manifest.json'); const lock=path.join(bundle,'.render-claim.lock'); if(!fs.existsSync(mf))continue; try{const m=JSON.parse(fs.readFileSync(mf,'utf8')); const claimedAt=Date.parse(m.render_claimed_at||''); const lockAge=fs.existsSync(lock)?Date.now()-fs.statSync(lock).mtimeMs:0; const stale=m.status==='render_claimed'&&Number.isFinite(claimedAt)&&(Date.now()-claimedAt)>=staleClaimMs; const orphanLock=fs.existsSync(lock)&&m.status!=='render_claimed'&&lockAge>=staleClaimMs; if(orphanLock)fs.unlinkSync(lock); if(stale&&m.exported_channel===channelId){m.status=m.upload_stage==='ready_to_upload'?'upload_uncertain':'recovery_required';m.recovery_required_at=new Date().toISOString();m.recovery_reason=m.upload_stage==='ready_to_upload'?'Upload may have reached YouTube; reconcile in Studio before requeue.':'Execution lease expired; inspect before requeue.';fs.writeFileSync(mf,JSON.stringify(m,null,2)+'\\n','utf8');try{fs.unlinkSync(lock);}catch(error){if(error?.code!=='ENOENT')throw error;}}}catch(error){throw new Error('Claim recovery failed for '+mf+': '+error.message);}}}
const files=fs.readdirSync(topicDir).filter(n=>/\\.md$/i.test(n)).map(n=>path.join(topicDir,n)).sort((a,b)=>fs.statSync(a).birthtimeMs-fs.statSync(b).birthtimeMs);
let selected=null;
for(const file of files){ const text=fs.readFileSync(file,'utf8'); const field=k=>text.match(new RegExp('^'+k+'=(.+)$','m'))?.[1]?.trim(); const bundle=field('SOURCE_BUNDLE'); if(!bundle)continue; const mf=path.join(bundle,'manifest.json'); if(!fs.existsSync(mf))continue; const manifest=JSON.parse(fs.readFileSync(mf,'utf8')); if(manifest.status==='md_ready'&&manifest.exported_channel===channelId&&!fs.existsSync(path.join(bundle,'.render-claim.lock'))){selected={file,text,bundle,mf,manifest};break;} }
if(!selected) throw new Error('No md_ready source bundle for '+channelId);
const brief=JSON.parse(fs.readFileSync(path.join(selected.bundle,'content-brief.json'),'utf8')); const source=JSON.parse(fs.readFileSync(path.join(selected.bundle,'source.json'),'utf8')); const evidence=JSON.parse(fs.readFileSync(path.join(selected.bundle,'evidence.json'),'utf8')); const transcript=JSON.parse(fs.readFileSync(path.join(selected.bundle,'transcript.json'),'utf8')); const rawCaption=fs.readFileSync(path.join(selected.bundle,'caption.txt'),'utf8').trim();
function sanitizeSourceText(value){return String(value||'').split(/\\r?\\n/).map(v=>v.trim()).filter(v=>v&&!/@[A-Za-z0-9_.]+|^#|팔로우|구독|좋아요|댓글|저장해|저장하세요|더 많은|프로필|링크/.test(v)).join('\\n');}
const caption=sanitizeSourceText(rawCaption);
const frame=path.join(selected.bundle,brief.visual_direction?.representative_frame||'keyframes/frame_001.jpg'); if(!fs.existsSync(frame))throw new Error('Representative frame missing: '+frame);
const evidenceMap=new Map([...(evidence.caption_sentences||[]).map(x=>[x.id,x.text]),...(transcript.segments||[]).map(x=>[x.id,x.text])]);
const ranks=(brief.rank_items||[]).slice(0,4).map((x,i)=>{const exact=(x.evidence_ids||[]).map(id=>evidenceMap.get(id)).filter(Boolean).map(v=>String(v).replace(/\\s+/g,' ').trim());if(!exact.length)throw new Error('Missing source evidence for rank '+(i+1));return {rank:i+1,name:x.name,reason:x.reason,caution:x.caution||'',evidence_ids:x.evidence_ids||[],source_details:exact};});
const captionLines=caption.split(/\\r?\\n/).map(v=>v.replace(/\\s+/g,' ').trim()).filter(Boolean); const originalTitle=String(selected.manifest.youtube_hook_title||captionLines[0]||brief.title), subtitle=brief.subtitle||captionLines.find(line=>line!==originalTitle)||'';
const rows=ranks.map(x=>x.name+' - '+x.reason).join('\\n');
function hookTitle(value,lane){const base=String(value||'').trim();if(/모르면|절대|꼭|손해|망치|대부분|돈 낭비|주의|경고|놓치/.test(base))return base.slice(0,58);const text=(base+' '+String(lane||'')).toLowerCase();let prefix='대부분 놓치고 있는 ';if(/피부|여드름|모공|스킨|선크림|레티|괄사|화장/.test(text))prefix='피부 망치기 전에 꼭 보세요: ';else if(/복용|타이레놀|인공눈물|의약품|약 /.test(text))prefix='약 먹고 쓰기 전에 꼭 확인하세요: ';else if(/영양제|비타민|오메가|성분|제품|고르는|비교/.test(text))prefix='돈 쓰기 전에 꼭 보세요: ';else if(/습관|루틴|신호|피로|스트레스|염증|코막힘/.test(text))prefix='몸이 보내는 신호, 그냥 넘기지 마세요: ';return (prefix+base).slice(0,58).replace(/[ :,-]+$/,'');}
const title=String(selected.manifest.youtube_hook_title||hookTitle(originalTitle,brief.lane)).slice(0,70);
const sourceDetails=ranks.map(x=>x.rank+'번 '+x.name+' 원문 근거: '+x.source_details.join(' / ')+(x.caution?' / 주의: '+x.caution:'')).join('\\n');
const imagePrompt=['Create one original 1080x1920 Korean Shorts visual for ${displayName}.','EDITORIAL PRIORITY: Read the FULL transcript and caption. Find the single most surprising, useful, concrete takeaway that makes the viewer understand why this video matters. The strongest numeric example, contrast, consequence, or counterintuitive conclusion usually outranks generic background explanation.','SPARSE MOBILE COPY: This is a fast-scrolling Short, not an article. Use one strong hook and only the minimum supporting copy needed to understand it. Target about 35-60 Korean characters of prominent visible copy total; never create dense paragraphs, tiny explanatory text, or a card packed with every source point. Omit secondary facts from the image when they weaken the main takeaway.','AUTONOMOUS CREATIVE DIRECTION: Choose the most natural visual structure yourself. Do not force a ranking, checklist, steps, comparison, or predefined template unless the source itself clearly requires it.','Source hook/title: '+originalTitle,'Existing brief core (advisory only; override it when the full transcript reveals a stronger takeaway): '+brief.core_message,'Existing content points (evidence pool, not mandatory visible sections):\\n'+rows,'Detailed evidence pool:\\n'+sourceDetails,'FULL SANITIZED CAPTION:\\n'+caption,'FULL TRANSCRIPT:\\n'+sanitizeSourceText(transcript.full_text||''),'Use exact concrete numbers and conditions from the source when they are the hook. Never invent or change a number. Preserve the source conclusion, but do not attempt to display every useful fact.','The finished image must be understandable at a glance within 1-2 seconds. Large Korean type, strong hierarchy, ample empty space.','BRAND SAFETY: Never show source creator names, pharmacist names, social handles, @usernames, profile photos, watermarks, follow/subscribe/save/comment calls to action, hashtags, logos, or source account branding. The only permitted channel branding is ${displayName}.'].join('\\n');
const description=[title,subtitle,'',...ranks.map(x=>x.rank+'위 '+x.name+' - '+x.reason),'',source.canonical_url||source.source_url||''].join('\\n');
const pinnedComment=['오늘 영상 핵심: '+title,'',...ranks.map(x=>x.rank+'번 '+x.name),'','이 중 가장 먼저 확인해보고 싶은 항목은 몇 번인가요? 댓글로 번호를 남겨주세요.','개인 상태에 따라 적용이 달라질 수 있으니 제품 설명서와 전문가 안내를 우선해 주세요.'].join('\\n');
const claimLock=path.join(selected.bundle,'.render-claim.lock');let claimHandle;try{claimHandle=fs.openSync(claimLock,'wx');fs.writeFileSync(claimHandle,JSON.stringify({channel_id:channelId,source_id:source.source_id,claimed_at:new Date().toISOString()}));fs.closeSync(claimHandle);}catch(error){if(claimHandle!==undefined)try{fs.closeSync(claimHandle);}catch{}throw new Error(error?.code==='EEXIST'?'Source bundle is already claimed: '+source.source_id:'Unable to claim source bundle: '+error.message);} selected.manifest.status='render_claimed'; selected.manifest.render_claimed_at=new Date().toISOString(); try{fs.writeFileSync(selected.mf,JSON.stringify(selected.manifest,null,2)+'\\n','utf8');}catch(error){try{fs.unlinkSync(claimLock);}catch{}throw error;}
const config={use_live_image:true,use_live_bgm:true,use_live_render:true,allow_youtube_upload:incoming.allow_youtube_upload===undefined?true:Boolean(incoming.allow_youtube_upload),youtube_privacy_status:incoming.youtube_privacy_status||'public',kie_image_model:incoming.kie_image_model||'gpt-image-2-text-to-image',kie_bgm_model:'V5_5',duration_seconds:Number(incoming.duration_seconds||5),local_render_dir:'C:/dev/n8n-youtube-shorts-automation/renders/source-reels',local_render_script:'C:/dev/n8n-youtube-shorts-automation/scripts/render-static-card.mjs',ffmpeg_path:'C:/Users/hjyeo/AppData/Local/Microsoft/WinGet/Packages/Gyan.FFmpeg_Microsoft.Winget.Source_8wekyb3d8bbwe/ffmpeg-8.1-full_build/bin/ffmpeg.exe',node_path:'C:/Program Files/nodejs/node.exe',poll_interval_seconds:30,bgm_retry_wait_seconds:90,image_poll_interval_seconds:30,image_retry_wait_seconds:30,image_task_retry_wait_seconds:30,image_poll_timeout_seconds:1800,image_poll_max_attempts:60,image_task_max_retries:2,youtube_category_id:'27',region_code:'KR'};
return [{json:{config,source_bundle:{channel_id:channelId,topic_md:selected.file,bundle_dir:selected.bundle,manifest_path:selected.mf,claim_lock:claimLock,source_id:source.source_id,representative_frame:frame},pack:{hook_title:title,subtitle,rank_items:ranks,tags:[...new Set([...(brief.tags||[]),'${displayName}','건강정보','쇼츠'])],description,pinned_comment:pinnedComment},image_payload:{model:config.kie_image_model,input:{prompt:imagePrompt,aspect_ratio:'9:16'}},bgm_payload:{prompt:'Calm trustworthy Korean wellness instrumental, soft piano and light acoustic texture, no vocals, no heavy drums.',model:config.kie_bgm_model,soundLoop:true,soundTempo:76,soundKey:'C',grabLyrics:false}}}];`;
}

const finalCode = `const fs=require('fs'); const path=require('path'); const data=$input.first().json; const sb=data.source_bundle||{}; let finalStatus=data.youtube?.url?'uploaded':'rendered'; try{if(sb.manifest_path&&fs.existsSync(sb.manifest_path)){const m=JSON.parse(fs.readFileSync(sb.manifest_path,'utf8')); m.status=finalStatus; m.upload_stage=data.youtube?.url?'complete':null; m.rendered_output=data.output_path||data.rendered_video_url||null; m.youtube_url=data.youtube?.url||m.youtube_url||null; m.youtube_video_id=data.youtube?.video_id||m.youtube_video_id||null; m.updated_at=new Date().toISOString(); fs.writeFileSync(sb.manifest_path,JSON.stringify(m,null,2)+'\\n','utf8'); if(sb.topic_md&&fs.existsSync(sb.topic_md)){const used=path.join(path.dirname(sb.topic_md),'사용완료');fs.mkdirSync(used,{recursive:true});const target=path.join(used,path.basename(sb.topic_md));if(!fs.existsSync(target))fs.renameSync(sb.topic_md,target);}}}finally{if(sb.claim_lock){try{fs.unlinkSync(sb.claim_lock);}catch(error){if(error?.code!=='ENOENT')throw error;}}} return [{json:{ok:true,status:finalStatus,channel_id:sb.channel_id,source_id:sb.source_id,output_path:data.output_path||null,youtube:data.youtube||{skipped:true},comment:data.comment||{created:false}}}];`;

function build(channelId, displayName, topicDir, outputFile, workflowId) {
  const nodes = template.nodes.filter(n=>keep.includes(n.name)).map(n=>structuredClone(n));
  if(channelId==='health_longevity'){
    const target=nodes.find(node=>node.name==='YouTube Upload Public');
    const source=longevityTemplate.nodes.find(node=>node.name==='YouTube Upload Public');
    target.credentials=structuredClone(source.credentials);
    const commentTarget=nodes.find(node=>node.name==='Post Top-Level Comment');
    const commentSource=longevityTemplate.nodes.find(node=>node.name==='Post Top-Level Comment');
    commentTarget.credentials=structuredClone(commentSource.credentials);
  }
  for(const node of nodes){if(node.parameters?.jsCode){node.parameters.jsCode=node.parameters.jsCode.replaceAll("$('Prepare Image and BGM Payloads')","$('Load Source Reel Bundle')");}}
  for(const node of nodes){if(node.parameters?.jsCode){node.parameters.jsCode=node.parameters.jsCode.replaceAll('Use flat editorial infographic composition, clear Korean title, numbered rows, no real faces, no logos, no medical treatment imagery.','Choose a simple sparse editorial composition that fits the source naturally. Do not force numbered rows, rankings, checklists, or a fixed template. No real faces, source logos, or medical treatment imagery.');}}
  const waitBgm=nodes.find(node=>node.name==='Wait BGM 30s'); if(waitBgm)waitBgm.parameters.amount='={{Number($json.config.poll_interval_seconds || 30)}}';
  const retryNodes=new Set(['KIE Create Image Task','KIE Get Image Task','KIE Get Image Task Retry','KIE Create BGM Task','KIE Get BGM Task','KIE Get BGM Task Retry','Local FFmpeg Render','Read Rendered MP4']);
  for(const node of nodes){if(retryNodes.has(node.name)){node.retryOnFail=true;node.maxTries=3;node.waitBetweenTries=node.name==='YouTube Upload Public'?10000:5000;}}
  const uploadNode=nodes.find(node=>node.name==='YouTube Upload Public'); if(uploadNode){delete uploadNode.retryOnFail;delete uploadNode.maxTries;delete uploadNode.waitBetweenTries;}
  const attachMp4=nodes.find(node=>node.name==='Attach Downloaded MP4'); if(attachMp4)attachMp4.parameters.jsCode=`const fs=require('fs');const base=$('Parse Local Render Result').first().json;const input=$input.first();const mf=base.source_bundle?.manifest_path;if(mf&&fs.existsSync(mf)){const m=JSON.parse(fs.readFileSync(mf,'utf8'));m.upload_stage='ready_to_upload';m.upload_ready_at=new Date().toISOString();fs.writeFileSync(mf,JSON.stringify(m,null,2)+'\\n','utf8');}return [{json:{...base,mp4_binary_ready:!!input.binary?.data},binary:input.binary}];`;
  const normalizeUpload=nodes.find(node=>node.name==='Normalize YouTube Upload'); if(normalizeUpload)normalizeUpload.parameters.jsCode=`const fs=require('fs'); const base=$('Attach Downloaded MP4').first().json; const upload=$input.first().json||{}; const videoId=upload.uploadId||upload.id||upload.videoId||upload.items?.[0]?.id||null; if(!videoId)throw new Error('YouTube upload returned no video ID; reconcile in Studio before retrying.'); const privacyStatus=base.config?.youtube_privacy_status||'public'; const youtube={skipped:false,privacy_status:privacyStatus,video_id:videoId,url:'https://www.youtube.com/watch?v='+videoId,raw:upload}; const mf=base.source_bundle?.manifest_path; if(mf&&fs.existsSync(mf)){const m=JSON.parse(fs.readFileSync(mf,'utf8'));m.status='uploaded';m.youtube_video_id=videoId;m.youtube_url=youtube.url;m.upload_committed_at=new Date().toISOString();fs.writeFileSync(mf,JSON.stringify(m,null,2)+'\\n','utf8');} return [{json:{...base,youtube}}];`;
  const commentNode=nodes.find(node=>node.name==='Post Top-Level Comment'); if(commentNode){commentNode.continueOnFail=true;commentNode.onError='continueRegularOutput';}
  nodes.unshift({id:crypto.randomUUID(),name:'Manual Trigger',type:'n8n-nodes-base.manualTrigger',typeVersion:1,position:[0,0],parameters:{}});
  nodes.splice(1,0,{id:crypto.randomUUID(),name:'Load Source Reel Bundle',type:'n8n-nodes-base.code',typeVersion:2,position:[240,0],parameters:{jsCode:loadCode(channelId,topicDir,displayName)}});
  nodes.push({id:crypto.randomUUID(),name:'Complete Source Reel Bundle',type:'n8n-nodes-base.code',typeVersion:2,position:[7600,0],parameters:{jsCode:finalCode}});
  const names=new Set(nodes.map(n=>n.name)); const connections={};
  for(const [from,value] of Object.entries(template.connections)){if(!names.has(from))continue; const v=structuredClone(value); for(const branch of v.main||[]) for(let i=branch.length-1;i>=0;i--) if(!names.has(branch[i].node))branch.splice(i,1); connections[from]=v;}
  connections['Manual Trigger']={main:[[{node:'Load Source Reel Bundle',type:'main',index:0}]]};
  connections['Load Source Reel Bundle']={main:[[{node:'Use Live Image?',type:'main',index:0}]]};
  connections['Normalize YouTube Upload']={main:[[{node:'Post Top-Level Comment',type:'main',index:0}]]};
  connections['Post Top-Level Comment']={main:[[{node:'Attach Comment Result',type:'main',index:0}]]};
  connections['Attach Comment Result']={main:[[{node:'Complete Source Reel Bundle',type:'main',index:0}]]};
  connections['Skip YouTube Upload']={main:[[{node:'Complete Source Reel Bundle',type:'main',index:0}]]};
  connections['Mock Render Result']={main:[[{node:'Complete Source Reel Bundle',type:'main',index:0}]]};
  delete connections['Final Result'];
  const workflow={id:workflowId,name:displayName+' - 원본 릴스 기반 쇼츠',nodes,connections,settings:{executionOrder:'v1'},active:false,versionId:crypto.randomUUID(),meta:{templateCredsSetupCompleted:true},tags:[]};
  fs.writeFileSync(path.join(root,'workflows',outputFile),JSON.stringify(workflow,null,2)+'\n','utf8');
}

build('haru_pharmacist','하루건강약사','C:/dev/n8n-youtube-shorts-automation/하루건강약사 소재','n8n_source_reel_haru_manual.json','66bce6ab603c5bef');
build('health_longevity','건강장수비결','C:/dev/n8n-youtube-shorts-automation/건강장수비결 소재','n8n_source_reel_longevity_manual.json','a8031b4a365c4603');
console.log('built 2 source reel workflows');
